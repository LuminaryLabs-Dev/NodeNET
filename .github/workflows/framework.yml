name: Avalonia Framework Acceptance

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'bridge/**'
      - 'bin/**'
      - 'test/integration/avalonia.test.mjs'
      - 'test/fixtures/avalonia-runtime/**'
      - 'scripts/lib/avalonia-runtime.mjs'
      - 'src/display/**'
      - 'src/services/display.js'
      - '.github/workflows/framework.yml'
      - 'package.json'
      - 'package-lock.json'

permissions:
  contents: read

env:
  NODENET_AVALONIA: '1'

jobs:
  avalonia-headless:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci --ignore-scripts
      - name: Restore, build, interact with, and render the locked Avalonia fixture
        env:
          NODENET_AVALONIA_OUTPUT: artifacts/avalonia-${{ runner.os }}
        run: npm run test:avalonia
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: avalonia-${{ runner.os }}
          path: artifacts/
          if-no-files-found: warn
